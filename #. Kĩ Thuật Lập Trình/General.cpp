#include <bits/stdc++.h>
using namespace std;

int main(){
    // 📌📌📌Day nhanh toc do nhap xuat du lieu
    ios_base ::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    // 📌📌📌Su dung ++i thay vi i++
    // 📌📌📌Su dung cout << "\n"; thay vi cout << endl;

    // 📌📌📌Xu li bit thay vi nhan va chia
    n << x; // Nhan n voi 2 ^ x, dich trai x bits
    n >> x; // Chia n voi 2 ^ x, dich phai x bits

    // 📌📌📌Kiem tra tinh chan le: ktra xem bit dau tien co phai la 0 hay kh
        // Neu phai thi la so chan
            if (n & 1) cout << "so l";
            else cout << "so chan";

    // 📌📌📌Su dung mang tinh thay vi vector
    // 📌📌📌Truyen tham so bang tham chieu
            // thay vi
                void func(vector<int> a);
            // Nen
                void func(vector<int> & a);
    // 📌📌📌Khi lap trinh ta nen chia (/) truoc roi moi nhan (*)
    
}